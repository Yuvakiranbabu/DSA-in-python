#split() inside the brackets we can give the delimeter where we say in which places to split
series='prison break,modern family,stranger things,dark,cobra kai'
series_list=series.split(',')# split is used to split the strings and they can be stored as elements in list
print(series_list)

# we can also use list() function to create a list from string